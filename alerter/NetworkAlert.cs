using System;

public class NetworkAlert
{
    public static int networkAlert(float celcius)
    {
        Console.WriteLine("ALERT: Temperature is {0} celcius", celcius);
        throw new NotImplementedException();
    }
}